#include <stdio.h>

int main() {
    int array[] = {34, 56, 78, 29};
    int size = sizeof(array) / sizeof(array[0]);
    int sum = 0;

    for (int i = 0; i < size; i++) {
        sum += array[i];
    }

    printf("The sum of all elements in the array is: %d\n", sum);

    return 0;
}
